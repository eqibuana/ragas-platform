# Copy to .env for local overrides.
# `npm run dev` (Vite) reads VITE_-prefixed vars directly from this file.
# `docker compose up` reads it too and bakes the value into the built bundle (see Dockerfile ARG).

VITE_API_BASE_URL=http://localhost:8000
